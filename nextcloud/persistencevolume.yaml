apiVersion: v1
kind: PersistentVolume
metadata:
  name: nextcloud
spec:
  storageClassName: nextcloud
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: /mnt/data/nextcloud
    server: k8s-node-1.tgp
