apiVersion: v1
kind: PersistentVolume
metadata:
  name: bitwarden
  labels:
    type: local
spec:
  storageClassName: bitwarden
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/bitwarden
