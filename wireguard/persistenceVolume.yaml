apiVersion: v1
kind: PersistentVolume
metadata:
  name: wireguard
spec:
  storageClassName: wireguard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: /mnt/data/wireguard
    server: k8s-nfs-1
