apiVersion: v1
kind: PersistentVolume
metadata:
  name: wireguard
  labels:
    type: local
spec:
  storageClassName: wireguard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/wireguard
