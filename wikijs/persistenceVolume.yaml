apiVersion: v1
kind: PersistentVolume
metadata:
  name: wikijs-data
spec:
  storageClassName: wikijs-data
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: /mnt/ZFS-SSD-460GB/SSD-tank/wikijs-data
    server: truenas-1.tgp
